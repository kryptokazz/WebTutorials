function doubleValue(num) {
    num *= 2; // Changing the value of the argument 'num'
    return num;
}

let num = 5;
console.log(doubleValue(num)); // Outputs: 10
console.log(num); // Outputs: 5 (the original value of 'num' remains unchanged)

